<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.simondiceperuanoBien.MainActivity" android:background="#877F7F">

    <TextView
            android:text="Simon dice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/titleText" android:textSize="44sp"
            android:layout_centerHorizontal="true"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:layout_marginTop="10dp"
            android:textColor="#FFFFFF"/>

    <ImageView
        android:id="@+id/starImage"
        android:layout_width="250dp"
        android:layout_height="115dp"
        android:layout_below="@id/titleText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        app:srcCompat="@android:drawable/ic_lock_silent_mode_off" />
    <Button
            android:text="Iniciar"
            android:layout_width="330dp"
            android:layout_height="300dp"
            android:id="@+id/startBtn" tools:text="Iniciar"
            android:textStyle="bold"
            android:textSize="40sp"
            android:layout_below="@id/starImage"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="70dp"
            android:background="#000000"
            android:textColor="#FFFFFF"/>
</RelativeLayout>